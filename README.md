# c25-project-example

An example of minimal project setup to avoid annoyances

## Setup & Installation

- Create and activate new virtual environment
- `pip install -r requirements.txt`

## Quality assurance

- Run tests with `pytest`
- Run Linting with `pylint *.py`